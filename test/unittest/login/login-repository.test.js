const expect = require("chai").expect;

describe("Given a username that exists in db", () => {
  describe("When call find user by username form repository", () => {
      it("Then should tell db to find in tinyoauth_user table", () => {
        
      })
  });
});
